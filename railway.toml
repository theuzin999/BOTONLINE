[build]
# Para ARM64 no Railway: instala Chromium + Chromedriver do sistema
nixPkgs = [
  "chromium",
  "chromedriver",
  # Dependências essenciais para o ChromeDriver rodar em ambiente Linux/headless:
  "libnss", 
  "libgconf",
  "libxss", 
  "alsa-lib",
  "nss"
]

[deploy]
startCommand = "python main.py"
